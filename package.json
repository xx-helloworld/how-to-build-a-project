{
  "name": "how-to-build-a-project",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "yarn jest",
    "release": "lerna publish"
  },
  "devDependencies": {
    "ts-jest": "^24.0.0",
    "jest": "^24.1.0",
    "lerna": "^3.13.0",
    "typescript": "^3.3.3333"
  }
}
